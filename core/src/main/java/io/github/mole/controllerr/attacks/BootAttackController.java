package io.github.mole.controllerr.attacks;

import io.github.mole.controllerr.environment.Registry;
import io.github.mole.mod.attacks.BootAttack;
import io.github.mole.mod.main.Board;

public class BootAttackController {
    public BootAttackController(Board board, BootAttack bootAttack, Registry registry) {
    }
}
